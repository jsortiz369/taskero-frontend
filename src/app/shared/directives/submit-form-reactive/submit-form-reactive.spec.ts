import { SubmitFormReactive } from './submit-form-reactive';

describe('SubmitFormReactive', () => {
  it('should create an instance', () => {
    const directive = new SubmitFormReactive();
    expect(directive).toBeTruthy();
  });
});
