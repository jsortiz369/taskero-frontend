import { ShowComponents } from './show-component';

describe('ShowComponents', () => {
  it('should create an instance', () => {
    const directive = new ShowComponents();
    expect(directive).toBeTruthy();
  });
});
