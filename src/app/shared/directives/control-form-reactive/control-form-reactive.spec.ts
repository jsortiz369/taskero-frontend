import { ControlFormReactive } from './control-form-reactive';

describe('ControlFormReactive', () => {
  it('should create an instance', () => {
    const directive = new ControlFormReactive();
    expect(directive).toBeTruthy();
  });
});
